apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-transcode-nfs
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 20Gi
  volumeName: "plex-transcode-nfs"